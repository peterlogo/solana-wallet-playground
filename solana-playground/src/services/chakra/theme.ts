import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
  colors: {
    primary: '#24243E',
    shade: '#32324C',
    secondary: '#DBDBEA'
  },
  fonts: {
    heading: 'Open Sans',
    body: 'Open Sans'
  }
});
